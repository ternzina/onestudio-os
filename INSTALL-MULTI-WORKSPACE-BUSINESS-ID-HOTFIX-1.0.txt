OneStudio OS Multi-workspace business_id hotfix 1.0

Contains:
supabase/migrations/20260729140000_multi_workspace_business_id_hotfix_1_0.sql

Purpose:
- fixes column reference "business_id" is ambiguous;
- allows a signed-in account to create its second and later isolated sites;
- keeps the already applied 20260729130000 migration immutable;
- does not delete or rewrite existing sites.

Install from the OneStudio OS project root, then inspect migration list:

  unzip -o "$HOME/Downloads/onestudio-multi-workspace-business-id-hotfix-1.0.zip" -d .
  npx supabase@beta migration list

Apply only after confirming 20260729140000 is local and remote is empty:

  npx supabase@beta db push
