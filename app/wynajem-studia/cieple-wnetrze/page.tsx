"use client";

import { LanguageProvider } from "../../../lib/language-provider";
import { HallPageContent } from "../hall-page-content";

export default function CiepleWnetrzePage() {
  return (
    <LanguageProvider>
      <HallPageContent hallKey="cieple-wnetrze" />
    </LanguageProvider>
  );
}
