OneStudio OS — Self-service Workspace hotfix 1.1

This replaces the failed local hotfix migration with a corrected version.
The failed migration was rolled back and was not recorded in the database.

Install and verify:

cd "/Users/zina/Desktop/onestudio-os-work" && \
unzip -o "$HOME/Downloads/onestudio-self-service-workspace-hotfix-1.1.zip" -d . && \
npx supabase@beta migration up && \
npx supabase@beta test db && \
rm -rf .next && \
npm run build
