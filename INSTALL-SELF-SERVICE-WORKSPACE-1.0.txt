OneStudio OS — Self-service Workspace 1.0

Adds:
- configurator → registration → launch flow;
- isolated workspace creation for a new authenticated owner;
- selected demo, palette, languages, currency and modules;
- dependency-safe module activation;
- draft public-site locales;
- duplicate-create protection.

Apply:
  npx supabase@beta migration up
  npx supabase@beta test db
  npm run build

